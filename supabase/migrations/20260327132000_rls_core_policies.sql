-- RLS policies require Supabase Auth (auth.uid) and are not applied on plain Postgres (e.g. db.prisma.io).
-- For Supabase-hosted databases, run `supabase/manual/rls_core_policies.sql` in the SQL Editor after migrations.
SELECT 1;
